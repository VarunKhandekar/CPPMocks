#ifndef SUBSTRING_H
#define SUBSTRING_H

int str_len(const char*);

bool is_prefix(const char*, const char*);

int substring_position(const char*, const char*);

int substring_position_recursion(const char*, const char*);


#endif